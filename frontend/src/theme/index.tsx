import { createTheme } from "@mui/material";

export const theme = createTheme( {

    palette: {
        primary: { main: '#FF9800' },
        secondary: { main: '#4CAF50' },
        background: {
            default: '#EEEEEE', 
            paper: '#FFFFFF',
        },
    },
});